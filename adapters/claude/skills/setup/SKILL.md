---
name: setup
description: Configure the standard SkipHow GitHub work surface for a repository by reusing or bootstrapping a minimal Project while preserving Issue-only degraded operation.
---

# setup

From the plugin root, read `plugins/skiphow/skills/setup/SKILL.md` and execute that canonical workflow unchanged. Resolve referenced files from the canonical skill directory.
