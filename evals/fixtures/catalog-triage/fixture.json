{
  "id": "catalog-triage",
  "summary": "The separable catalog modules plus a mixed owner inbox of observed bugs and unaccepted product ideas for read-only normalization and backlog design.",
  "kind": "checked-in overlay",
  "synthetic": true,
  "derives_from": "catalog-audit",
  "planted": [
    "INBOX.md mixes two observed defects, one duplicate symptom, two product ideas, and one weak style observation.",
    "The underlying catalog modules let the run check whether the defect observations share causes.",
    "No idea is marked accepted and the prompt asks for an actionable backlog without implementation."
  ],
  "absent_on_purpose": [
    "No existing tracker, priority, owner decision, or authorization to change code.",
    "No instruction that every inbox line must become an item."
  ],
  "setup": [
    "Copy the base fixture catalog-audit into an empty scratch directory.",
    "Copy the files in this directory over it.",
    "Run: git init && git add . && git -c user.email=fixture@example.invalid -c user.name=Fixture commit -m 'Fixture base'"
  ],
  "run_checks": "python -m pytest tests/catalog_checks.py",
  "end_state_signals": [
    "The report separates observed defects from unaccepted ideas and weak observations.",
    "Duplicate symptoms are grouped only where live code supports one cause, and the Git tree remains unchanged."
  ],
  "privacy": "Invented catalog and feedback, no personal data, credentials, or network use."
}
