# Example accounts module

A small ledger for a savings product. It is a fixture, not a product.

There are no automated checks. Import the modules to see that they load:

    python -c "import accounts.ledger, accounts.export"
