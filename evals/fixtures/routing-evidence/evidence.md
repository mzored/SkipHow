# Lane evidence

These are synthetic project records for a routing decision. They describe no actual provider performance. Model and effort availability must come from the active host. The requested result is a recommendation, not an executed assignment.

## Bounded conversion

An independent lane converts a frozen table of 400 public enum names to a second documented schema. All inputs and mapping rules fit in one short reference. An existing independent checker compares every output against the approved mapping in seconds. Errors have no external effects and can be retried locally. Context isolation saves the lead from reading the table. If actual coordination would still cost more than direct execution, direct work is a valid recommendation. No history establishes a particular model's adequacy.

## Routine migration

A lane titled implementation migrates historical accounting records. Acceptance preserves balances, tax rounding, and reversal semantics across three interacting historical schema versions. Current tests cover only the newest schema; older mistakes could remain hidden until an audit. Restoration after live use would require reconstructing subsequent transactions. No live access or migration is authorized. Routing must account for the missing verification evidence and contextual reasoning, even though the task title sounds routine. A stronger route alone cannot establish safety or authorize execution.

## Missing rule

A lower-cost assignment converted values using whole-unit rounding. The delegate brief omitted the required currency precision table. The complete table exists in project evidence but was never supplied or made discoverable to the delegate. The return consistently follows the incomplete brief; there is no evidence of reasoning failure. The next brief can include the table without increasing context materially.

## Excessive context

A lower-cost assignment tried to reconcile three independent archival catalogs in one context. The transcript's truncation marker shows the last catalog was excluded. The returned work is correct for the two visible catalogs. The catalogs have independent acceptance checks and can be reconciled separately before integration. No evidence shows that increased reasoning effort would restore the missing input.

## Capability miss

A lower-cost assignment had the complete short specification, all tools, and a contained counterexample checker. Its answer asserted that rounding each item and then summing always equals rounding the sum. The checker supplied a counterexample; the return repeated the same invalid equivalence while acknowledging the counterexample. No missing input or context truncation is recorded. A retry needs a justified capability or effort change, a different decomposition that removes the reasoning bottleneck, or a direct lead solution. Repeating the same assignment unchanged has no supporting evidence.
