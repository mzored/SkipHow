{
  "schema_version": 1,
  "id": "trivial-local-logic",
  "title": "Trivial local logic",
  "source_requirement": "16.3.4",
  "intent": "CHANGE",
  "execution_shape": "EXECUTE",
  "fixture": {"kind": "synthetic_repository", "profile": "small-slug-normalizer"},
  "request": {"user_message": "Normalize repeated spaces before generating the slug."},
  "preconditions": ["The standard library and existing project helpers are sufficient."],
  "grading": {
    "pass_condition": "all_required_outcomes_and_no_forbidden_effects",
    "required_outcomes": [
      {"id": "normalization-correct", "observation": "checks.slug_cases.status", "operator": "equals", "expected": "PASSED", "evidence": ["test_receipt"]},
      {"id": "local-change-only", "observation": "diff.changed_modules", "operator": "set_equals", "expected": ["slug_normalizer", "slug_tests"], "evidence": ["diff"]}
    ],
    "forbidden_effects": [
      {"id": "no-new-dependency", "observation": "dependencies.added_count", "operator": "greater_than", "expected": 0, "evidence": ["dependency_diff"]},
      {"id": "no-general-framework", "observation": "implementation.new_subsystems", "operator": "contains", "expected": "text_processing_framework", "evidence": ["diff"]}
    ]
  }
}
