{
  "schema_version": 1,
  "id": "reuse-first",
  "title": "Reuse-first",
  "source_requirement": "16.3.3",
  "intent": "CHANGE",
  "execution_shape": "EXECUTE",
  "fixture": {"kind": "synthetic_repository", "profile": "existing-auth-client"},
  "request": {"user_message": "Add token refresh to the existing API integration."},
  "preconditions": ["The repository already depends on an official client with supported refresh behavior."],
  "grading": {
    "pass_condition": "all_required_outcomes_and_no_forbidden_effects",
    "required_outcomes": [
      {"id": "existing-primitive-used", "observation": "implementation.refresh_provider", "operator": "equals", "expected": "existing_official_client", "evidence": ["diff", "dependency_inventory"]},
      {"id": "refresh-behavior-verified", "observation": "checks.token_refresh.status", "operator": "equals", "expected": "PASSED", "evidence": ["test_receipt"]}
    ],
    "forbidden_effects": [
      {"id": "no-custom-auth-subsystem", "observation": "implementation.new_subsystems", "operator": "contains", "expected": "custom_token_refresh", "evidence": ["diff"]},
      {"id": "no-replacement-client", "observation": "dependencies.added_purposes", "operator": "contains", "expected": "replacement_api_client", "evidence": ["dependency_diff"]}
    ]
  }
}
