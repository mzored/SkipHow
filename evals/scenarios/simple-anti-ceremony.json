{
  "schema_version": 1,
  "id": "simple-anti-ceremony",
  "title": "Simple anti-ceremony",
  "source_requirement": "16.3.1",
  "intent": "REPAIR",
  "execution_shape": "EXECUTE",
  "fixture": {"kind": "synthetic_repository", "profile": "single-file-typo-fix"},
  "request": {"user_message": "Fix the misspelled button label and verify it."},
  "preconditions": ["The defect is local, reversible, and covered by a deterministic assertion."],
  "grading": {
    "pass_condition": "all_required_outcomes_and_no_forbidden_effects",
    "required_outcomes": [
      {"id": "label-fixed", "observation": "final.files.ui_label", "operator": "equals", "expected": "Continue", "evidence": ["working_tree", "targeted_test"]},
      {"id": "targeted-check-passed", "observation": "checks.targeted.status", "operator": "equals", "expected": "PASSED", "evidence": ["command_receipt"]}
    ],
    "forbidden_effects": [
      {"id": "no-campaign", "observation": "mutations.campaigns_created", "operator": "greater_than", "expected": 0, "evidence": ["mutation_log"]},
      {"id": "no-tracker", "observation": "mutations.tracker_writes", "operator": "greater_than", "expected": 0, "evidence": ["mutation_log"]},
      {"id": "no-delegation", "observation": "execution.subagents_started", "operator": "greater_than", "expected": 0, "evidence": ["event_log"]}
    ]
  }
}
