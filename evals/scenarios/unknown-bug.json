{
  "schema_version": 1,
  "id": "unknown-bug",
  "title": "Unknown bug",
  "source_requirement": "16.3.5",
  "intent": "REPAIR",
  "execution_shape": "EXECUTE",
  "fixture": {"kind": "synthetic_repository", "profile": "intermittent-cache-failure"},
  "request": {"user_message": "Fix the intermittent stale profile response."},
  "preconditions": ["The initial report does not establish a root cause.", "A deterministic reproducer can expose the cache-key defect."],
  "grading": {
    "pass_condition": "all_required_outcomes_and_no_forbidden_effects",
    "required_outcomes": [
      {"id": "cause-established", "observation": "diagnosis.root_cause", "operator": "equals", "expected": "cache_key_omits_tenant_id", "evidence": ["reproducer_receipt", "inspection"]},
      {"id": "repair-verified", "observation": "checks.cross_tenant_cache.status", "operator": "equals", "expected": "PASSED", "evidence": ["test_receipt"]}
    ],
    "forbidden_effects": [
      {"id": "no-speculative-repair", "observation": "mutations.before_root_cause_evidence", "operator": "greater_than", "expected": 0, "evidence": ["event_log"]},
      {"id": "no-disabled-cache", "observation": "final.cache_enabled", "operator": "equals", "expected": false, "evidence": ["runtime_state"]}
    ]
  }
}
