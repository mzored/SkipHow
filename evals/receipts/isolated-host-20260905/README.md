# Isolated Codex diagnostics, September 5, 2026

These receipts record Codex CLI 0.153.0 with the exact SkipHow 4.1.0 package,
payload `e79048fdcd69ebe9d282dd7068ace85c6d4ec21d44ced98a1569a7fd7a43d55c`.
The owner authenticated through the native isolated login flow. Separate
operating-system and Codex homes kept personal instructions out, and the approved
Git marketplace source supplied the installed package. Claude's isolated profile
remained unauthenticated.

| Receipt | Bounded observation |
| --- | --- |
| [Control](codex-control.json) | Read-only project answer with the package uninstalled; no candidate or personal skill context and preserved fixture bytes. |
| [Ordinary-language activation](codex-bootstrap.json) | The complete kernel loaded at event item 5 before edits. Four catalog repairs reached the synthetic origin. The host budget interrupted the session before final reporting; its `interrupted` terminal remains unchanged. |
| [Delivery verification](codex-delivery-verification.json) | Independent deterministic replay of the synthetic remote passed all four repairs. Foreign files were preserved, the publication marker was absent, and verification clones were removed. This was not independent delegate review. |
| [Native resume](codex-resume.json) | Reconciled the existing delivery and completed reporting without repeating completed changes or publishing. |
| [Unrelated](codex-unrelated.json), [discussion](codex-discussion.json), [analysis](codex-analysis.json) | The arithmetic and tool-discussion requests did not load the kernel. Project analysis loaded it and explained the shipping defect without edits. |
| [Native compaction](codex-compact-native.json), [continuation](codex-post-compaction.json) | The host completed a native `contextCompaction` item; the following read-only turn retained delivery status. The earlier delivery-verification scope predates this separate compaction check. |

The captures retain configuration, prompts, sanitized events, final artifacts and
their hashes. Their conservative `UNVERIFIED` evidence labels remain separate
from the individual observations above. The control used a different prompt and
does not establish improvement over the base host.

No independent delegate review, durable failed-delegate recovery, real GitHub
tracker behavior, canonical CTO suite coverage or reliability rate was
demonstrated. These remain 4.1.0 receipts. The planned 4.1.1 release repairs
receipt privacy and does not change the runtime behavior tested here.

Account quota and authentication metadata are omitted, host identifiers are
consistently pseudonymized, and private home prefixes are replaced. Per-run usage
and fixture evidence remain available. The [privacy remediation record](../../../docs/evidence.md#receipt-privacy-remediation)
states the checked scope and the unresolved GitHub PR-reference/cache purge;
sanitization is not a guarantee that every historical copy is private-data-free.
