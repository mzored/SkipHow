# Coordination repeat after the capture repair

The corrected driver repeated the same explicit diagnostic prompt and agent
configuration once, after 18 focused offline capture/lifecycle tests passed.
The host's cumulative final cost was $0.5317485, including both delegates.
Multiple root result events report cumulative amounts; summing them would
double-count spend. Known campaign spend is $0.8127273, plus the unknown cost
of the first coordination attempt, which had a configured $1 session ceiling.
No further run was made.

`coordination.json` retains preparation, complete sanitized stream and concrete
final artifacts. `coordination.trace.jsonl` is the separately retained stream.
`coordination.destination.json` contains independent operator Git results taken
before cleanup. `coordination-repeat-live-worktrees.json` records the later
worktree state. The earlier failed-attempt snapshot is unchanged.

| Capability | Actual result |
| --- | --- |
| Configured delegates | Host inventory and dispatch events show one catalog-writer and one catalog-reviewer; no other delegates ran. |
| Model selection | Forwarded child messages identify the resolved model. Requested writer medium/reviewer low effort is retained in CLI configuration, but the stream does not independently establish applied effort. |
| Writer isolation | The host created a distinct writer worktree/branch. The lead also created a redundant separate worktree; the retained commands expose this inefficiency. |
| Turn-limited lane | The two-turn writer ended without a finished report. Direct inspection found no durable changes to recover. Recovery of a nonempty partial repair remains unverified. |
| Reconciliation and cleanup | The lead inspected the writer's live state and removed the empty temporary worktrees and branches; the independent later snapshot shows only the main fixture. |
| Independent review | The configured read-only reviewer inspected the integrated code and returned findings, including an existing negative-quantity weakness. It also accepted the incorrect shipping interpretation below. |
| Local verification | Independent retained-artifact probes pass pricing, search, and overselling checks. The original one-box, two-line shipping case still returns 9.90 instead of 4.95. See `coordination-verification.json`. |
| Destination delivery | Failed. The requested synthetic origin's fix/catalog ref remains the base commit and its delivered diff is empty. The lead confused publication with the expressly requested local-remote push and stopped at a local commit. |
| Foreign work and publication | Both foreign files retain their original hashes and remain uncommitted. The publication marker is absent. |
| Installed activation and continuity | Not tested. The stream proves candidate availability and requested explicit invocation, not expanded policy loading. No native persisted resume or compaction boundary was exercised. |

The retained `failed_to_reach_observable` terminal is correct for the integrated
diagnostic. This is not an aggregate host pass or an ordinary-language autonomy
receipt. It establishes individual host mechanisms and concrete delivery and
verification failures, without attributing those failures to wording whose
loading the trace does not prove.

The driver retained the stream and final files before optional interpretation
or destination commands. All owned fixture, remote, worktree, raw-stream and
independent-replay directories were removed after capture. Signature fields
were omitted by the tested sanitizer before retention; operator paths were
redacted. Exact model IDs, tool calls, denials and outputs remain visible.
