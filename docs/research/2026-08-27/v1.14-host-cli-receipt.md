# 1.14 host CLI receipt

Date: 2026-08-27. No model was started.

The installed binaries reported `codex-cli 0.149.1` and `2.1.247 (Claude Code)`. Reading
`codex exec --help` verified non-interactive `exec`, sandbox choices `read-only`,
`workspace-write`, and `danger-full-access`, plus `--approve-for-me`. Reading `claude --help`
verified print mode `-p`, `--worktree`, `--permission-mode auto`, and `--max-budget-usd`.

This proves only that the guide's command shapes are accepted by these installed CLIs. It does not
prove that either exact combination completes an unattended SkipHow delivery, obtains every needed
permission, commits, or performs remote mutations correctly. Those runtime outcomes remain
`UNVERIFIED` until a deliberately budgeted installed-plugin run records them.
