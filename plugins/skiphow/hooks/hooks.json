{
  "description": "SkipHow continuity: print load and reload instructions at session boundaries without changing authority.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|clear",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'SkipHow is installed. When a project request matches the skiphow skill description, load its owner kernel, then read the repository instructions and live state before acting.'",
            "timeout": 10
          }
        ]
      },
      {
        "matcher": "compact|resume",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'If SkipHow governed this request, reload its owner kernel and current authoritative context before continuing.'",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
