interface:
  display_name: "Idea"
  short_description: "Capture one idea in the canonical tracker"
  default_prompt: "Use $idea to capture this thought without shaping or implementing it."
policy:
  allow_implicit_invocation: true
