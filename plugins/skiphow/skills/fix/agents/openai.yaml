interface:
  display_name: "Fix"
  short_description: "Repair defects with risk-based rigor"
  default_prompt: "Use $fix to repair this defect with verification proportional to its risk."
policy:
  allow_implicit_invocation: true
