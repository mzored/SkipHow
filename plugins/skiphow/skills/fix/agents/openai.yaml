interface:
  display_name: "Fix"
  short_description: "Repair defects with proportionate evidence"
  default_prompt: "Use $fix to repair this defect with evidence proportional to its changed surfaces."
policy:
  allow_implicit_invocation: true
