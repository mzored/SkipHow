interface:
  display_name: "SkipHow"
  short_description: "Handle product and software work"
  default_prompt: "Use $skiphow to handle this project request and verify the result."
policy:
  allow_implicit_invocation: true
