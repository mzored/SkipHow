interface:
  display_name: "SkipHow"
  short_description: "Deliver product and software work"
  default_prompt: "Use $skiphow to carry this project request through to a verified result."
policy:
  allow_implicit_invocation: true
