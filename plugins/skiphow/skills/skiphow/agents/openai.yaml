interface:
  display_name: "SkipHow"
  short_description: "Own project work from one plain-language request"
  default_prompt: "Own this project request, make the technical decisions, and verify the result."
policy:
  allow_implicit_invocation: true
