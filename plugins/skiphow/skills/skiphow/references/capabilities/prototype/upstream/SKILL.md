---
name: prototype
description: Build a throwaway prototype to answer a design question. Use when the user wants to sanity-check whether a state model or logic feels right, or explore what a UI should look like.
---

# Prototype

A prototype is throwaway code that answers one design question. It is clearly marked, trivial to run, in-memory by default, intentionally unpolished, and exposes the relevant state. Once the question is settled, the production branch retains the validated decision rather than the disposable implementation.
