interface:
  display_name: "Preflight"
  short_description: "Check SkipHow delivery prerequisites"
  default_prompt: "Use $preflight to report READY, SETUP_NEEDED, or DEGRADED for this repository without changing it."
policy:
  allow_implicit_invocation: true
